const database = '{{ mongodb_db }}';
const user = '{{ mongodb_user }}';
const pass = '{{ mongodb_password }}';

if (database && user) {
  db.createUser({
    user: user,
    pwd: pass,
    roles: [
      {
        role: 'dbAdmin',
        db: database
      }
    ]
  });
}